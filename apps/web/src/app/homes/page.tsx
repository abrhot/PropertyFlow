import { HomesPage } from '@/features/listings/homes-page';

export const metadata = {
  title: 'Available homes — PropertyFlow',
  description: 'Browse vacant apartments and homes to rent or buy.',
};

export default function Page() {
  return <HomesPage />;
}
